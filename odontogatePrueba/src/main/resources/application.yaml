spring:
  datasource:
      url: jdbc:postgresql://localhost:5432/odontogate_db
      username: admin
      password: admin123
      driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
